# Figuras Básicas y Paths en Canvas 2D

Este proyecto es una aplicación web desarrollada con **HTML5**, **JavaScript** y **Bootstrap 5**, cuyo objetivo es demostrar el uso del elemento `<canvas>` para el dibujo de figuras 2D y el trazado manual de formas mediante la API de Canvas.

La interfaz presenta un diseño moderno, responsivo y ordenado, sin necesidad de hacer scroll, integrando componentes visuales como Navbar, Cards y Footer.

---

## 🎯 Objetivo del Proyecto

Mostrar de forma práctica y visual el funcionamiento de las principales funciones del contexto 2D de Canvas, combinando figuras geométricas básicas y el uso de *paths* para crear formas personalizadas.

---

## 🧩 Funcionalidades Implementadas

En el canvas se renderizan los siguientes elementos:

- **Rectángulo relleno** usando `fillRect()`
- **Limpieza parcial del área** usando `clearRect()`
- **Contorno de un rectángulo** usando `strokeRect()`
- **Triángulo** creado mediante la API de trazado:
  - `beginPath()`
  - `moveTo()`
  - `lineTo()`
  - `fill()`

Además, el canvas es **responsivo**, ajustando su tamaño automáticamente al contenedor y redibujando las figuras cuando la ventana cambia de tamaño.

---

## 🖥️ Interfaz de Usuario

- Navbar superior con título del proyecto  
- Cards para organizar la información y el área de dibujo  
- Botón para redibujar las figuras en tiempo real  
- Footer con información del programador  
- Diseño fijo sin desplazamiento vertical (sin scroll)

---

## 🛠️ Tecnologías Utilizadas

- HTML5  
- CSS3  
- JavaScript (Canvas API)  
- Bootstrap 5  

---

## 👨‍💻 Autor

**Víctor Pinedo**  
Proyecto académico – Ejemplos de Canvas 2D  
© 2026

---

## 📌 Notas

Este proyecto sirve como base para futuras extensiones, tales como animaciones con `requestAnimationFrame()`, interacción con el usuario o visualización de colisiones y efectos gráficos.
